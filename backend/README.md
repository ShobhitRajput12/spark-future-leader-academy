# Spark Future Leader Academy (Backend)

Node.js + Express API with Gemini + MongoDB logging.

## Run

```bash
npm install
npm run dev
```

## Endpoints

- `POST /ai` body: `{ "prompt": "How to join NDA?" }`
- `GET /schemes`
