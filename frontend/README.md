# Spark Future Leader Academy (Frontend)

Expo React Native app.

## Run

```bash
npm run start
```

## Backend URL

Set `EXPO_PUBLIC_API_BASE_URL` (see `.env.example`) so the AI Chat can call `POST /ai`.

